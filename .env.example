cat > .env.example <<'EOF'
# Example env file for School Swap. Copy to .env.local and fill values.

VITE_GEMINI_API_KEY=your_api_key_here
# VITE_SUPABASE_URL=...
# VITE_SUPABASE_ANON_KEY=...
EOF

git add .env.example
git commit -m "docs: add .env.example"
git push origin main
